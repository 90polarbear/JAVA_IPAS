create table  eaxm1 (
  `id` INT NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(75) NOT NULL,
  `drink` VARCHAR(75) NOT NULL,
  `pearl` VARCHAR(75) NOT NULL,
  `number` INT NOT NULL,
  PRIMARY KEY (`id`));
  
  
  
  
  mysql -u sarah -p00000
  use testdb;
